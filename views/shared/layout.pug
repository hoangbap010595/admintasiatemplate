doctype html
html
  include head.pug

  body
    #page_wrapper
      include page_wrapper.pug
      script(type='text/javascript' src='js/ui/ui.tabs.js')
      script(type='text/javascript').
        $(document).ready(function() {
          // Tabs
          $('#tabs, #tabs2, #tabs5').tabs();
        });

      #page-layout
        #page-content
          #page-content-wrapper
            block content

            block sidebar

    include footer.pug
